const compoNome = document.getElementById('nomo');
const formulario = document.getElementById('from');
const botaoEviar = document.getElementById('btn-enviar')


botaoEviar?.addEventListener('click', (e: MouseEvent) => {                   //  ?  foi colocado para informar que o   dEventListener    só ira retorna caso o  
    
})                                                                          ///valor não seja nulo, só acesamos  a propriedade caso o elemento seja valido